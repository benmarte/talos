#!/usr/bin/env bash
# gemini CLI stub for talos tests. Same logging contract as the codex stub.
{
  printf 'GEMINI ARGS:'; for a in "$@"; do printf ' [%s]' "$a"; done; printf '\n'
} >> "${RUNNER_LOG:-/dev/null}"
echo "gemini-stub-ok"
