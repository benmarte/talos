#!/usr/bin/env bash
# claude CLI stub for talos tests. Same logging contract as the codex stub.
{
  printf 'CLAUDE ARGS:'; for a in "$@"; do printf ' [%s]' "$a"; done; printf '\n'
} >> "${RUNNER_LOG:-/dev/null}"
echo "claude-stub-ok"
