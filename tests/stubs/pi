#!/usr/bin/env bash
# pi CLI stub for talos tests. Same logging contract as the codex stub.
{
  printf 'PI ARGS:'; for a in "$@"; do printf ' [%s]' "$a"; done; printf '\n'
} >> "${RUNNER_LOG:-/dev/null}"
echo "pi-stub-ok"
