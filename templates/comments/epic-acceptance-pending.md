${HEADER}

**Epic acceptance pending** — all sub-issues are closed, but this epic's own acceptance criteria still have unticked boxes:

${DETAILS}

Leaving the epic open. A human should tick the boxes or file follow-up work; the epic will auto-close on a future sweep once `check-epic-acceptance` reports none remaining.
