${HEADER}

**Review:** ${VERDICT} — ${SUMMARY}

${DETAILS}

**Human-attention report** — what a human should specifically look at before trusting or overriding this verdict (highest-risk first; `file:line` with every item; "nothing requires human attention beyond the diff" when the list is empty):

${ATTENTION_REPORT}
